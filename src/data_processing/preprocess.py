import json
import re
import emoji

def preprocess(raw_data_path:str='../../data/raw_data.jsonl'):
	# TODO:
	# - Generate data.csv (Ex: ../../data/sample_data.csv)
	# - Generate labels.json (Ex: ../../data/sample_labels.json)
	pass